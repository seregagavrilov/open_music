[uwsgi]
module = {{ uwsgi_module }}.wsgi:application
uid = {{ user }}

chdir = {{ project_path }}
master = true
processes = {{ uwsgi_processes_amount }}

socket = myapp.sock
chmod-socket = 664
vacuum = true

die-on-term = true